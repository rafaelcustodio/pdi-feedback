## Codebase Patterns
- Prisma schema at `prisma/schema.prisma`, generated client at `src/generated/prisma/` (gitignored)
- Server Actions pattern: `src/app/(dashboard)/[feature]/actions.ts` for mutations/queries
- Access control: always use `getEffectiveAuth()` from `@/lib/impersonation` (never `auth()` directly)
- Employee form: `src/components/employee-form.tsx` — main form for creating/editing employees
- Profile page: `src/app/(dashboard)/perfil/` — employee self-service view
- Email: `src/lib/email.ts` + `src/lib/email-templates.ts` — Nodemailer with SMTP fallback to console
- Notifications: existing `Notification` model in Prisma schema
- Dark mode: always include `dark:` Tailwind variants
- Timezone: use UTC methods for dates from server (getUTCDate, etc.)
- Prisma generate workaround: `PRISMA_SCHEMA_ENGINE_BINARY=/tmp/schema-engine npx prisma generate` (create mock schema-engine first: `echo '#!/bin/sh' > /tmp/schema-engine && chmod +x /tmp/schema-engine`)
- Migration files must be created manually (no network access to binaries.prisma.sh for schema-engine)
- Pre-existing lint errors (7 errors, 8 warnings) and 8 test failures in sector-schedule-utils.test.ts — not caused by our changes
---

## 2026-03-03 - US-001
- Implemented: Added 8 new enums (Ethnicity, Gender, MaritalStatus, EducationLevel, ContractType, HealthPlanOption, ShirtSize, BankAccountOption) and 30+ optional fields to User model
- Files changed: `prisma/schema.prisma`, `prisma/migrations/20260303100000_add_hr_enums_and_personal_fields/migration.sql`
- **Learnings for future iterations:**
  - `src/generated/prisma/` is gitignored — don't try to `git add` it
  - Network access to `binaries.prisma.sh` is blocked — must use mock schema-engine binary for `prisma generate`
  - Migration SQL must be written manually since `prisma migrate dev` also requires the schema-engine binary
  - Prisma 7 uses `provider = "prisma-client"` (not `prisma-client-js`) — WASM-based generation
  - Pre-existing: 8 test failures in `sector-schedule-utils.test.ts` and 7 lint errors in unrelated files
---

## 2026-03-03 - US-002
- Implemented: Added Dependent and EmergencyContact models to Prisma schema with proper relations to User
- Files changed: `prisma/schema.prisma`, `prisma/migrations/20260303110000_add_dependent_and_emergency_contact/migration.sql`
- **Learnings for future iterations:**
  - Dependent model: id (cuid), userId, name, relationship, cpf (optional), createdAt, updatedAt
  - EmergencyContact model: id (cuid), userId, name, phone, relationship (optional), createdAt, updatedAt
  - Both have `@@index([userId])` for efficient queries by user
  - Relations added to User as `dependents Dependent[]` and `emergencyContacts EmergencyContact[]`
---

## 2026-03-03 - US-003
- Implemented: Added ChangeRequestStatus enum (pending/approved/rejected) and ChangeRequest model with dual relations to User (requester via "ChangeRequestUser" and reviewer via "ChangeRequestReviewer")
- Files changed: `prisma/schema.prisma`, `prisma/migrations/20260303120000_add_change_request/migration.sql`
- **Learnings for future iterations:**
  - ChangeRequest model: id (cuid), userId, fieldName, oldValue?, newValue?, status (default pending), reviewedById?, reviewedAt?, createdAt
  - Two relations on User: `changeRequests` (as requester) and `reviewedChangeRequests` (as reviewer) — use named relations "ChangeRequestUser" and "ChangeRequestReviewer"
  - Indexes on userId, status, and reviewedById for efficient queries
  - Foreign key on reviewedById uses ON DELETE SET NULL (reviewer is optional)
---

## 2026-03-03 - US-004
- Implemented: Updated prisma/seed.ts with complete HR data for 3 users (Admin, CEO, Vinicius Pacheco), 7 dependents across 3 users, and 4 emergency contacts across 3 users
- Files changed: `prisma/seed.ts`
- **Learnings for future iterations:**
  - Seed cleanup order must include `changeRequest.deleteMany()`, `dependent.deleteMany()`, and `emergencyContact.deleteMany()` before `user.deleteMany()` (FK constraints)
  - All new enums must be imported from `../src/generated/prisma/client` in seed.ts
  - `hobbies` field is `String[]` — pass as JS array in Prisma create
  - `socialNetworks` field is `Json?` — pass as plain JS object
  - `createMany` works well for bulk-creating dependents and emergency contacts per user
---
