# Ralph Progress Log
Started: Wed Mar  4 09:48:17 PST 2026

## Codebase Patterns
- Prisma migrations are manually created SQL files in `prisma/migrations/<timestamp>_<name>/migration.sql`
- Run `npm run prisma:generate` after schema changes (no DB needed for client generation)
- Pre-existing lint errors exist (7 errors, 11 warnings) — not related to this feature branch
- All 81 tests pass as baseline before this feature work
- Database is not available in dev container — migrations must be created manually

---

## 2026-03-04 - US-001
- Added `OnboardingType` enum with values `manager_feedback` and `hr_conversation` to schema.prisma
- Added nullable `onboardingType OnboardingType?` field to `Feedback` model
- Created migration `20260304150000_add_onboarding_type`
- Prisma client generated successfully, typecheck passes, all 81 tests pass
- Files changed: `prisma/schema.prisma`, `prisma/migrations/20260304150000_add_onboarding_type/migration.sql`
- **Learnings for future iterations:**
  - No PostgreSQL in dev container — cannot run `prisma migrate dev`, must create migration SQL manually
  - Migration format: `CREATE TYPE "EnumName" AS ENUM (...)` then `ALTER TABLE "TableName" ADD COLUMN ...`
  - Prisma generate works without DB connection
---

## 2026-03-04 - US-002
- Removed automatic onboarding feedback creation from createEmployee and updateEmployee
- Removed `generateOnboarding` flag from updateEmployee signature and client form
- Removed onboarding confirmation modal from employee-form.tsx
- Removed `showOnboardingModal` state and `getOnboardingInfo` helper
- Removed unused `admissionChanged` variable
- Updated import to only keep `handleSectorTransfer` from sector-schedule-utils
- No test changes needed — no existing tests covered auto onboarding creation
- Files changed: `src/app/(dashboard)/colaboradores/actions.ts`, `src/components/employee-form.tsx`
- **Learnings for future iterations:**
  - The employee-form.tsx had a modal that prompted users to generate onboarding feedbacks — this was tied to the `isPending` prop and `generateOnboarding` param
  - The `isPending` prop still exists on EmployeeForm but is no longer used for onboarding modal — may be useful for US-005 button visibility
  - `createOnboardingFeedbacks` and `updateOnboardingFeedbacks` still exist in sector-schedule-utils.ts but are no longer imported by actions.ts
---

## 2026-03-04 - US-003
- Implemented `generateOnboardingSchedules(employeeId)` server action in `src/app/(dashboard)/colaboradores/actions.ts`
- Admin-only via `requireAdmin()` helper
- Validates: admissionDate exists, manager assigned (via active EmployeeHierarchy), <= 90 days since admission, no existing pending onboarding feedbacks
- Calculates 4 dates (45d and 90d from admission), adjusts weekends via `snapToBusinessDay` from sector-schedule-pure-utils
- Skips dates that already passed
- Creates Feedback records with `isOnboarding=true`, correct `onboardingType` (manager_feedback / hr_conversation)
- For manager_feedback: managerId = employee's manager; for hr_conversation: managerId = admin (session user)
- Returns created schedules with dates, period, and type
- Added `snapToBusinessDay` import from sector-schedule-utils (re-exported from pure-utils)
- Exported `OnboardingScheduleResult` type for UI consumption
- Files changed: `src/app/(dashboard)/colaboradores/actions.ts`
- **Learnings for future iterations:**
  - `requireAdmin()` is a local helper in actions.ts that calls `getEffectiveAuth()` and checks for admin role
  - Feedback model uses `managerId` field (not `authorId`) — for HR conversations, set managerId to the admin who triggered the action
  - `snapToBusinessDay` is exported from `sector-schedule-pure-utils.ts` and re-exported via `sector-schedule-utils.ts`
  - Use `prisma.feedback.createMany` for batch creation of feedbacks
  - The existing `createOnboardingFeedbacks` in sector-schedule-utils.ts used a similar pattern but without onboardingType
---

## 2026-03-04 - US-004
- Created comprehensive test file `src/lib/__tests__/generate-onboarding-schedules.test.ts` with 10 tests
- Tests cover: happy path (4 feedbacks), weekend adjustment, >90 days error, missing admissionDate, missing manager, existing pending feedbacks, 45d past (only 90d created), authorId correctness, non-admin rejection, employee not found
- All 91 tests pass (10 new + 81 existing), typecheck clean
- Files changed: `src/lib/__tests__/generate-onboarding-schedules.test.ts`
- **Learnings for future iterations:**
  - Mock `@/lib/sector-schedule-utils` with both `snapToBusinessDay` and `handleSectorTransfer` since actions.ts imports both
  - Mock `@/lib/schedule-utils` with `removeScheduledFeedbackEvents` since it's imported by actions.ts
  - Use `vi.useFakeTimers()` + `vi.setSystemTime()` to control `new Date()` in the action for date comparisons
  - The action file is large and imports many dependencies — all must be mocked even if not used by the tested function
---

## 2026-03-04 - US-005
- Added "Gerar Agendamentos de Onboarding" button to employee-form.tsx
- Button visible only for admin users in edit mode, placed in "Dados do Sistema" section
- Button disabled with reason text when: no admissionDate, no managerId, or >90 days since admission
- Confirmation dialog shows 4 date previews (Feedback Gestor 45d/90d, Conversa RH 45d/90d)
- Past dates shown with "(data passada — não será criado)" indicator in gray
- Color-coded: blue for manager feedback, purple for HR conversation, gray for past dates
- Loading state during action call, success/error messages displayed
- Dark mode fully supported (dialog, button, badges)
- Added `currentUserRole` prop to EmployeeForm, passed from [id]/page.tsx
- Imported `generateOnboardingSchedules` action and `CalendarPlus` icon
- All 91 tests pass, typecheck clean
- Files changed: `src/components/employee-form.tsx`, `src/app/(dashboard)/colaboradores/[id]/page.tsx`
- **Learnings for future iterations:**
  - The edit employee page (`[id]/page.tsx`) already checks for admin role, but passing `currentUserRole` as prop allows the client component to conditionally render admin-only UI
  - Client-side date preview uses a local `snapWeekend` helper (same logic as `snapToBusinessDay`) since pure-utils is server-side
  - No toast library exists in this project — use inline success/error messages
  - The form uses a `sectionClass` constant for consistent card styling
---
