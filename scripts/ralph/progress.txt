# Ralph Progress Log
Started: Mon Mar  2 11:01:25 PST 2026

## Codebase Patterns
- Prisma partial unique indexes must be added via raw SQL migrations (Prisma schema doesn't support WHERE clause on indexes)
- Use `IF NOT EXISTS` on CREATE INDEX in migrations for idempotency
- Use `DISTINCT ON` with `ORDER BY` for "pick best per group" queries in PostgreSQL
- Prisma 7 uses TypeScript-based generated client in `src/generated/prisma/` - schema model changes require `prisma generate`, but database-only changes (indexes) don't affect the generated client
- Pre-existing lint errors on main: theme-provider setState-in-effect error, unused vars in programacao/actions.ts, pdi-tracking.tsx, sector-schedule-utils
---

## 2026-03-02 - US-001
- Created migration `20260302190000_unique_active_pdi_per_employee` that:
  - Reparents PDIFollowUp from non-keeper active PDIs to keeper (most recent updatedAt)
  - Re-runs consolidation of PDIGoal and PDIComment for safety (idempotent)
  - Cancels non-keeper active PDIs
  - Adds unique partial index: `CREATE UNIQUE INDEX IF NOT EXISTS idx_pdi_one_active_per_employee ON PDI(employeeId) WHERE status = 'active'`
- Files changed:
  - `prisma/migrations/20260302190000_unique_active_pdi_per_employee/migration.sql` (new)
  - `scripts/ralph/prd.json` (US-001 passes: true)
  - `scripts/ralph/progress.txt` (this entry)
- **Learnings for future iterations:**
  - Previous migration (20260302160000) consolidated goals and comments but missed PDIFollowUp (added in later migration 20260302170000)
  - Network-restricted env: `prisma migrate dev` and `prisma generate` fail due to missing Linux engine binaries (only Windows cached). Migration SQL files can still be created manually.
  - The PDI model uses `status` enum (active/cancelled) - partial unique index ensures one active PDI per employee at DB level
---

## 2026-03-02 - US-002
- Modified `getOrCreatePDI()` in `src/app/(dashboard)/pdis/actions.ts` to handle race conditions:
  - Wrapped `prisma.pDI.create()` in try/catch
  - On P2002 (unique constraint violation), falls back to `findFirst()` for the existing active PDI
  - Non-P2002 errors are re-thrown
- Files changed:
  - `src/app/(dashboard)/pdis/actions.ts` (modified getOrCreatePDI)
  - `scripts/ralph/prd.json` (US-002 passes: true)
  - `scripts/ralph/progress.txt` (this entry)
- **Learnings for future iterations:**
  - Prisma P2002 error code is for unique constraint violations - check `error.code === "P2002"`
  - For Prisma errors in "use server" actions, use `error && typeof error === "object" && "code" in error` pattern for safe type narrowing
  - The `getOrCreatePDI()` function is the primary entry point for creating PDIs in the continuous model
---
