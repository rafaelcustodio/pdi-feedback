# Ralph Progress Log
Started: Mon Mar  2 16:11:11 PST 2026

## Codebase Patterns
- Prisma generate requires `PRISMA_SCHEMA_ENGINE_BINARY=/dev/null` env var workaround in this environment (no network for engine download)
- Migration SQL must be created manually and applied via `prisma db execute` or direct DB connection since `prisma migrate dev` needs the schema engine binary
- Generated Prisma client files are in `src/generated/prisma/` and have `@ts-nocheck` — they're regenerated via `prisma generate`
- All pre-existing lint errors are in: theme-provider.tsx (setState in effect), programacao/actions.ts, pdi-tracking.tsx, sector-schedule-utils.ts — not from our changes

---

## 2026-03-02 - US-001
- What was implemented: Added Microsoft OAuth token fields (msAccessToken, msRefreshToken, msTokenExpiresAt) to User model, outlookEventId to PDIFollowUp and Feedback models
- Files changed:
  - prisma/schema.prisma — added 3 fields to User, 1 to PDIFollowUp, 1 to Feedback
  - prisma/migrations/20260302200000_add_outlook_calendar_fields/migration.sql — migration SQL
  - src/generated/prisma/* — regenerated Prisma client
- **Learnings for future iterations:**
  - All new fields are nullable (String?, DateTime?) so no data migration needed
  - Database is not running in this environment — migration file is created but must be applied when DB is available
  - `PRISMA_SCHEMA_ENGINE_BINARY=/dev/null npx prisma generate` works to regenerate client without network
---
